import 'package:flutter/material.dart';

class GridOne extends StatelessWidget {
  const GridOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar:AppBar(title: const Text("Grid One"),) ,
        body: Container());
  }
}